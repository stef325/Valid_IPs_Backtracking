AllValidIps(stringRecebida,indice, contador, IP){

    se(o tamanho da stringRecebida = index e contador = 4){
        retira o ponto do final
        imprima(ip)
        retorne;
    }

    se(o tamanho da stringRecebida é menor que o indice +1){
        retorne;
    }

    para (i = 0 enquanto i < 3; i++){ 
        se o tamanho da stringRecebida é menor que indice +2 ou (se i = 1 e caractere do indice atual é = 0){
            retorne;
        }
        se o tamanho da stringRecebida é menor que indice +3 ou se o digito formado pelos 3 caracteres a partir do indice > 255{
            retorne;
        }

        IP < proximo caractere da stringRecebida a partir do i+1 e adiciona um ponto

        AllValidIps(stringRecebida,i+1,contador+1, IP)

        IP < retira i+2 caracteres
    }
    
    

}